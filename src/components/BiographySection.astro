<section id="biografia" class="relative z-20 py-32 px-6" data-bio>
  <div class="container mx-auto max-w-5xl">
    <div class="grid md:grid-cols-2 gap-12 items-center">
      <!-- Image -->
      <div
        class="relative transition-all duration-1000 opacity-0 -translate-x-10"
        data-bio-image
      >
        <div class="aspect-[3/4] bg-card overflow-hidden">
          <img
            src="/bio.jpg"
            alt="Luna Kuyen - Retrato Luz y Compas"
            class="w-full h-full object-cover grayscale hover:grayscale-0 transition-all duration-700"
          />
        </div>
        <div
          class="absolute -bottom-4 -right-4 w-32 h-32 border-2 border-primary -z-10"
        >
        </div>
      </div>

      <!-- Content -->
      <div
        class="transition-all duration-1000 delay-300 opacity-0 translate-x-10"
        data-bio-content
      >
        <h2 class="text-3xl md:text-4xl font-bold mb-6 text-foreground">
          Biografía
        </h2>
        <div class="space-y-4 text-muted-foreground leading-relaxed">
          <p>
            Artista escénica apasionada por crear experiencias únicas que
            conectan con el público desde la emoción y la sorpresa. Su formación
            en danzas variadas, teatro físico, clown, pantomima, circo y
            maquillaje artístico la ha convertido en una intérprete versátil
            capaz de adaptarse a cualquier proyecto: desde intervenciones con
            fuego que iluminan eventos corporativos, fiestas y shows musicales,
            hasta personajes cautivadores para eventos escolares, celebraciones
            privadas y propuestas escénicas experimentales. Además, ha
            desempeñado su rol docente tanto en danzas como en teatro, generando
            espacios pedagógico-didácticos que se adaptan a cualquier sujeto de
            aprendizaje.
          </p>
          <p>
            Su perspectiva se basa en impulsar un arte versátil que posibilite
            la fusión con diversas disciplinas y formatos, con el objetivo de
            generar comunidad, trabajar en equipo y enriquecer propuestas.
          </p>
          <p>
            El compromiso, la disposición y el ideal de generar momentos
            memorables le permiten llevar magia y profesionalismo a cada
            proyecto, superar expectativas y crear conexiones genuinas con cada
            audiencia.
          </p>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  const root = document.querySelector("[data-bio]") as HTMLElement | null;
  const img = document.querySelector("[data-bio-image]") as HTMLElement | null;
  const content = document.querySelector(
    "[data-bio-content]"
  ) as HTMLElement | null;

  if (root && img && content) {
    const observer = new IntersectionObserver(
      ([entry]) => {
        if (entry.isIntersecting) {
          img.classList.remove("opacity-0");
          img.classList.remove("-translate-x-10");
          img.classList.add("opacity-100");
          img.classList.add("translate-x-0");

          content.classList.remove("opacity-0");
          content.classList.remove("translate-x-10");
          content.classList.add("opacity-100");
          content.classList.add("translate-x-0");
        }
      },
      { threshold: 0.2 }
    );
    observer.observe(root);
  }
</script>
